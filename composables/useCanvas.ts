export const useCanvas = () => {
	const createHero = (name:string, frame:string, si:number, furn:number, engr:number) => {

	}

	const createRoster = (heroList:Array<object>) => {

	}

	return {
		createHero,
		createRoster
	}
}